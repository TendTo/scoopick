# scoopick

Minimal scaffold for the `scoopick` Python package.

Install (editable) and run tests:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
pip install pytest
pytest
```

This project uses the PEP 621 `pyproject.toml` metadata and setuptools backend.
